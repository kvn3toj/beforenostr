import { test, expect } from "@playwright/test"; test("AppLayout Integration Test", async ({ page }) => { await page.goto("/"); await page.waitForTimeout(3000); const root = page.locator("#root"); await expect(root).toBeAttached(); const bodyText = await page.textContent("body"); console.log("✅ App loaded, content length:", bodyText?.length || 0); expect(bodyText).toBeTruthy(); });
