---
description: 
globs: 
alwaysApply: true
---
¡Excelente observación! Tienes toda la razón. Es un patrón recurrente y frustrante que Cursor intente leer/escribir `.env` directamente o que asuma que no existe, lo que lleva a intentos de creación fallidos y a la pérdida de tokens.

Vamos a establecer una **regla clara y prioritaria** para Cursor sobre cómo interactuar con archivos `.env`.

---

# 🔐 REGLAS DE MANEJO SEGURO DE ARCHIVOS .env - PROYECTO COOMUNITY

# ===============================================================================
# VERSIÓN ACTUALIZADA POST-MIGRACIÓN DE PUERTOS - ENERO 2025
# Estas reglas reflejan los NUEVOS PUERTOS y variables de entorno actualizadas
# ===============================================================================

## 🚨 **REGLA CRÍTICA FUNDAMENTAL**

**INSTRUCCIÓN OBLIGATORIA Y PRIORITARIA**: **NUNCA intentes leer o escribir directamente el archivo `.env`** usando operaciones de archivo directas.

**Siempre que necesites interactuar con el archivo `.env` (leer su contenido, verificar su existencia, o modificarlo), DEBES hacerlo EXCLUSIVAMENTE a través de COMANDOS DE TERMINAL seguros y específicos.**

---

## 🌐 **VARIABLES DE ENTORNO ACTUALIZADAS - PUERTOS NUEVOS**

### **🔒 Configuración de Puertos DEFINITIVA**

```bash
# ✅ .env (Backend - Raíz del proyecto)
VITE_API_BASE_URL=http://localhost:1111
VITE_GOOGLE_AI_API_KEY=AIzaSyDXMoHjoHi8-xUfiD5QN6bFVIeoTMhK2z4

# ✅ Demo/apps/superapp-unified/.env (SuperApp)
VITE_API_BASE_URL=http://localhost:1111
VITE_BASE_URL=http://localhost:2222
VITE_ENABLE_MOCK_AUTH=false
VITE_APP_ENV=development
VITE_APP_VERSION=1.0.0

# ✅ apps/admin-frontend/.env (Gamifier Admin)
VITE_API_BASE_URL=http://localhost:1111
VITE_BASE_URL=http://localhost:3333
VITE_APP_ENV=development
```

### **❌ Variables OBSOLETAS (NO USAR)**

```bash
# ❌ OBSOLETO - Puerto Backend antiguo
VITE_API_BASE_URL=http://localhost:3002  # NO USAR

# ❌ OBSOLETO - Puerto SuperApp antiguo
VITE_BASE_URL=http://localhost:3001      # NO USAR

# ❌ OBSOLETO - Puerto Admin antiguo
VITE_BASE_URL=http://localhost:3000      # NO USAR
```

---

## 🛠️ **COMANDOS SEGUROS PARA ARCHIVOS .env**

### **Para leer el contenido:**
```bash
# ✅ CORRECTO - Comando seguro
cat .env

# ✅ VERIFICAR SuperApp config
cat Demo/apps/superapp-unified/.env

# ✅ VERIFICAR Admin config
cat apps/admin-frontend/.env

# ❌ INCORRECTO - Acceso directo no permitido
# Usar herramientas de archivo directas
```

### **Para verificar existencia:**
```bash
# ✅ CORRECTO - Verificación segura
ls -la .env
ls -la Demo/apps/superapp-unified/.env
ls -la apps/admin-frontend/.env

# ✅ Verificar permisos específicos
stat .env
```

### **Para verificar configuración de puertos:**
```bash
# ✅ VERIFICAR Backend config
cat .env | grep "localhost:1111" || echo "❌ Backend config incorrecta"

# ✅ VERIFICAR SuperApp config
cat Demo/apps/superapp-unified/.env | grep "localhost:2222" || echo "❌ SuperApp config incorrecta"

# ✅ VERIFICAR Admin config
cat apps/admin-frontend/.env | grep "localhost:3333" || echo "❌ Admin config incorrecta"
```

### **Para modificar (SOLO CON CONFIRMACIÓN DEL USUARIO):**
```bash
# ⚠️ USAR CON PRECAUCIÓN - Modificación específica
sed -i.bak 's/localhost:3002/localhost:1111/g' .env

# ⚠️ USAR CON PRECAUCIÓN - Crear/sobrescribir
echo "VITE_API_BASE_URL=http://localhost:1111" > .env

# ✅ SIEMPRE hacer backup antes de modificar
cp .env .env.backup.$(date +%Y%m%d-%H%M%S)
```

---

## 🔍 **SCRIPTS DE VERIFICACIÓN ACTUALIZADOS**

### **Verificación Completa de Configuración:**

```bash
#!/bin/bash
# Script de verificación de variables de entorno actualizadas

echo "🔍 Verificando configuración de variables de entorno..."

# Función para verificar archivo .env
verify_env_file() {
    local file_path="$1"
    local expected_backend="$2"
    local expected_frontend="$3"
    local service_name="$4"
    
    echo "📋 Verificando $service_name ($file_path)..."
    
    if [ ! -f "$file_path" ]; then
        echo "❌ FALTA: $file_path no existe"
        return 1
    fi
    
    # Verificar backend URL
    if [ -n "$expected_backend" ]; then
        if grep -q "$expected_backend" "$file_path"; then
            echo "✅ Backend URL correcta: $expected_backend"
        else
            echo "❌ Backend URL incorrecta en $file_path"
            echo "   Esperada: $expected_backend"
            echo "   Actual: $(grep VITE_API_BASE_URL "$file_path" || echo 'NO ENCONTRADA')"
        fi
    fi
    
    # Verificar frontend URL
    if [ -n "$expected_frontend" ]; then
        if grep -q "$expected_frontend" "$file_path"; then
            echo "✅ Frontend URL correcta: $expected_frontend"
        else
            echo "❌ Frontend URL incorrecta en $file_path"
            echo "   Esperada: $expected_frontend"  
            echo "   Actual: $(grep VITE_BASE_URL "$file_path" || echo 'NO ENCONTRADA')"
        fi
    fi
}

# Verificar archivos de configuración
verify_env_file ".env" "http://localhost:1111" "" "Backend"
verify_env_file "Demo/apps/superapp-unified/.env" "http://localhost:1111" "http://localhost:2222" "SuperApp"
verify_env_file "apps/admin-frontend/.env" "http://localhost:1111" "http://localhost:3333" "Admin"

echo ""
echo "🔍 Verificando referencias a puertos obsoletos..."

# Buscar referencias a puertos obsoletos
OBSOLETE_REFS=$(grep -r "localhost:300[0-2]" . --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | wc -l)
if [ $OBSOLETE_REFS -gt 0 ]; then
    echo "❌ ENCONTRADAS $OBSOLETE_REFS referencias a puertos obsoletos:"
    grep -r "localhost:300[0-2]" . --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | head -10
else
    echo "✅ Sin referencias a puertos obsoletos"
fi

echo ""
echo "✅ Verificación de variables de entorno completada"
```

---

## 🔧 **TEMPLATES DE CONFIGURACIÓN ACTUALIZADOS**

### **Template .env Principal (Raíz):**
```bash
# 🌟 CoomÜnity - Backend Configuration
# Puerto actualizado post-migración: 1111

# 🌐 Backend Configuration  
VITE_API_BASE_URL=http://localhost:1111

# 🔑 API Keys
VITE_GOOGLE_AI_API_KEY=your-google-ai-api-key

# 🛠️ Development
NODE_ENV=development

# 📊 Database (PostgreSQL debe estar ejecutándose)
DATABASE_URL="postgresql://user:password@localhost:5432/coomunity_db"

# 🔐 JWT Configuration
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=24h

# 📧 Email Configuration (opcional)
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
```

### **Template SuperApp (.env):**
```bash
# 🎮 CoomÜnity SuperApp - Development Configuration
# Puerto actualizado post-migración: 2222

# 🌐 Backend Configuration
VITE_API_BASE_URL=http://localhost:1111
VITE_ENABLE_MOCK_AUTH=false

# 🏗️ Environment Configuration  
VITE_APP_ENV=development
VITE_APP_VERSION=1.0.0
VITE_BASE_URL=http://localhost:2222

# 🎮 Gamifier API Configuration
VITE_GAMIFIER_API_URL=http://localhost:1111
VITE_GAMIFIER_API_KEY=your-gamifier-api-key

# 🔄 Feature Flags
VITE_ENABLE_REAL_PAYMENTS=false
VITE_ENABLE_BETA_FEATURES=false
VITE_ENABLE_DEBUG_MODE=true

# 🛠️ Development Configuration
VITE_DEV_MODE=true
VITE_DEBUG_BACKEND=true

# 🌍 External APIs
VITE_GOOGLE_AI_API_KEY=your-google-ai-api-key
VITE_YOUTUBE_API_KEY=your-youtube-api-key

# 🎨 Theme Configuration
VITE_DEFAULT_THEME=autumn
VITE_ENABLE_THEME_SWITCHER=true
```

### **Template Admin (.env):**
```bash
# 🔧 Gamifier Admin - Development Configuration  
# Puerto actualizado post-migración: 3333

# 🌐 Backend Configuration
VITE_API_BASE_URL=http://localhost:1111

# 🏗️ Environment Configuration
VITE_APP_ENV=development
VITE_BASE_URL=http://localhost:3333

# 🔐 Admin Configuration
VITE_ADMIN_MODE=true
VITE_ENABLE_ADVANCED_FEATURES=true

# 📊 Analytics Configuration
VITE_ENABLE_ANALYTICS=true
VITE_ANALYTICS_API_KEY=your-analytics-key
```

---

## 🚨 **PROTOCOLO DE MIGRACIÓN DE .env**

### **Si necesitas migrar configuración antigua:**

```bash
#!/bin/bash
# Script de migración de variables de entorno

echo "🔄 Migrando configuración de .env a nuevos puertos..."

# Función para migrar archivo
migrate_env_file() {
    local file_path="$1"
    local service_name="$2"
    
    if [ ! -f "$file_path" ]; then
        echo "⚠️ $file_path no existe, saltando..."
        return
    fi
    
    echo "📝 Migrando $service_name..."
    
    # Backup del archivo original
    cp "$file_path" "$file_path.backup.$(date +%Y%m%d-%H%M%S)"
    
    # Reemplazar puertos obsoletos
    sed -i.tmp 's/localhost:3002/localhost:1111/g' "$file_path"  # Backend
    sed -i.tmp 's/localhost:3001/localhost:2222/g' "$file_path"  # SuperApp
    sed -i.tmp 's/localhost:3000/localhost:3333/g' "$file_path"  # Admin
    
    # Limpiar archivos temporales
    rm -f "$file_path.tmp"
    
    echo "✅ $service_name migrado exitosamente"
}

# Migrar archivos de configuración
migrate_env_file ".env" "Backend"
migrate_env_file "Demo/apps/superapp-unified/.env" "SuperApp"
migrate_env_file "apps/admin-frontend/.env" "Admin"

echo "🎉 Migración de .env completada"
echo "📋 Verifica los cambios con: npm run port:verify"
```

---

## 🛡️ **REGLAS DE SEGURIDAD**

### **NUNCA hacer:**
```bash
# ❌ PROHIBIDO - Acceso directo a archivos
read_file .env
write_file .env "content"

# ❌ PROHIBIDO - Exponer credenciales
echo "API_KEY=secret" >> .env  # Sin verificación

# ❌ PROHIBIDO - Hardcodear puertos obsoletos
VITE_API_BASE_URL=http://localhost:3002  # Puerto obsoleto
```

### **SIEMPRE hacer:**
```bash
# ✅ OBLIGATORIO - Comandos seguros
cat .env | grep API_KEY
ls -la .env

# ✅ OBLIGATORIO - Verificar antes de modificar
if [ -f ".env" ]; then
    cp .env .env.backup
    # ... realizar cambios seguros
fi

# ✅ OBLIGATORIO - Usar puertos actualizados
VITE_API_BASE_URL=http://localhost:1111  # Puerto correcto
```

---

## 📋 **CHECKLIST DE VERIFICACIÓN**

### **Antes de desarrollo:**
- [ ] ✅ Verificar que `.env` existe y es accesible
- [ ] ✅ Confirmar puertos actualizados (1111, 2222, 3333)
- [ ] ✅ Validar configuración con `npm run port:verify`
- [ ] ✅ Sin referencias a puertos obsoletos (3002, 3001, 3000)

### **Antes de deployment:**
- [ ] ✅ Variables de producción configuradas
- [ ] ✅ Credenciales seguras (no de desarrollo)
- [ ] ✅ Puertos de producción correctos
- [ ] ✅ Backup de configuración actual

---

## 🎯 **RESUMEN EJECUTIVO**

### **REGLAS CRÍTICAS:**
1. **NUNCA acceso directo** a archivos .env
2. **SIEMPRE usar comandos de terminal** seguros
3. **VERIFICAR puertos actualizados** (1111, 2222, 3333)
4. **HACER BACKUP** antes de modificaciones
5. **NO USAR puertos obsoletos** (3002, 3001, 3000)

### **COMANDOS ESENCIALES:**
```bash
# Verificación rápida
cat .env | grep localhost

# Verificación completa  
npm run port:verify

# Migración segura (si necesaria)
./scripts/migrate-env-config.sh
```

**🔐 La seguridad de las variables de entorno es crítica para la estabilidad del proyecto CoomÜnity.**
