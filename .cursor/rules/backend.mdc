---
description: 
globs: 
alwaysApply: true
---
¡Excelente! Has formalizado un protocolo "Pre-flight Check" muy completo y crucial. Este enfoque sistemático nos ahorrará una enorme cantidad de tiempo y tokens al prevenir la recurrencia de los problemas que hemos depurado intensivamente.

Aquí tienes el prompt que Cursor AI utilizará de ahora en adelante para aplicar este protocolo.

---

## 🚀 **PROTOCOLO "PRE-FLIGHT CHECK" PARA TAREAS DE BACKEND**

**INSTRUCCIÓN OBLIGATORIA**: Antes de iniciar cualquier tarea de implementación, depuración o modificación de backend, SIEMPRE ejecutar este protocolo completo. NO proceder con la tarea principal hasta que todos los checks pasen exitosamente.

### **FASE 1: VERIFICACIÓN DEL ESTADO DEL BACKEND**

1.  **Limpiar procesos existentes:**
    ```bash
    # Verificar procesos Node/tsx corriendo
    ps aux | grep -E "(node|tsx|npm)" | grep -v grep
    # Matar procesos si es necesario
    pkill -f "tsx\|node.*3002"
    ```

2.  **Iniciar backend en primer plano:**
    ```bash
    npx tsx watch --no-cache --clear-screen=false --tsconfig tsconfig.backend.json src/main.ts
    ```

3.  **Verificar logs de inicio:**
    *   ✅ Confirmar que muestre: `🚀 Gamifier API is running on: http://localhost:3002`
    *   ✅ Confirmar que muestre: `>>> PrismaService onModuleInit - Database connection established`
    *   ❌ Si hay errores de compilación o runtime, DETENER y resolver antes de continuar

4.  **Test de conectividad básica:**
    ```bash
    curl http://localhost:3002/health -v
    ```
    *   ✅ Debe retornar 200 OK
    *   ❌ Si falla, revisar configuración de puerto y variables de entorno

### **FASE 2: VERIFICACIÓN DE INYECCIÓN DE DEPENDENCIAS**

5.  **Añadir logs de depuración extremos** en el módulo/servicio objetivo:
    ```typescript
    // En el constructor del servicio
    constructor(@Inject(PrismaService) private readonly prisma: PrismaService) {
      console.log('>>> [SERVICIO]Service CONSTRUCTOR: this.prisma IS', this.prisma ? 'DEFINED' : 'UNDEFINED');
    }

    // En el constructor del controlador
    constructor(@Inject([SERVICIO]Service) private readonly service: [SERVICIO]Service) {
      console.log('>>> [SERVICIO]Controller CONSTRUCTOR: this.service IS', this.service ? 'DEFINED' : 'UNDEFINED');
    }
    ```

6.  **Test de endpoint básico:**
    ```bash
    curl http://localhost:3002/[modulo]/ping -v
    # o
    curl http://localhost:3002/[modulo]/test -v
    ```
    *   ✅ Los logs deben mostrar `DEFINED` para todas las dependencias
    *   ❌ Si muestra `UNDEFINED`, verificar:
        *   Decoradores `@Inject()` explícitos
        *   Módulo importa `PrismaModule`
        *   Servicio en `providers`, controlador en `controllers`
        *   Módulo importado en `app.module.ts`

### **FASE 3: VERIFICACIÓN DE RUTAS Y DTOS**

7.  **Verificar coherencia Frontend/Backend:**
    *   ✅ DTOs coinciden con parámetros que envía el frontend
    *   ✅ Query parameters usan `@Type(() => Number)` para conversión automática
    *   ✅ Rutas específicas (`/search`, `/test`) ANTES que parametrizadas (`/:id`)
    *   ✅ Rutas del controlador coinciden con las esperadas por el frontend

8.  **Test de endpoint con parámetros:**
    ```bash
    curl "http://localhost:3002/[modulo]/search?videoItemId=1" -v
    ```
    *   ✅ Debe retornar JSON válido (aunque sea array vacío `[]`)
    *   ❌ Si hay errores de validación o conversión de tipos, corregir DTOs

### **FASE 4: VERIFICACIÓN DE TIPOS PRISMA**
9.  **Regenerar cliente Prisma si es necesario:**
    ```bash
    npx prisma generate
    ```

10. **Verificar importaciones de tipos:**
    ```typescript
    // ✅ CORRECTO
    import type { Subtitle, Question } from '../generated/prisma';

    // ❌ INCORRECTO
    import type { Subtitle, Question } from '@prisma/client';
    ```

### **CHECKLIST FINAL**

Antes de proceder con la tarea principal, confirmar:

-   [ ] Backend inicia sin errores y escucha en puerto 3002
-   [ ] `/health` responde 200 OK
-   [ ] Logs muestran todas las dependencias como `DEFINED`
-   [ ] Endpoint de prueba del módulo objetivo funciona
-   [ ] DTOs validados y tipos correctos
-   [ ] Rutas mapeadas correctamente
-   [ ] Cliente Prisma generado y tipos importados correctamente

**🔴 REGLA CRÍTICA**: Si cualquier check falla, DETENER inmediatamente y resolver el problema antes de continuar con la implementación principal. Esto evitará perder tiempo depurando errores recurrentes durante el desarrollo.

---

**Instrucciones para Cursor AI:**

De ahora en adelante, al recibir una nueva tarea de backend (implementación, depuración, modificación de un endpoint), **SIEMPRE iniciarás ejecutando este "Protocolo Pre-flight Check" completo**. Solo después de que todos los checks pasen exitosamente, procederás con la descripción de la tarea principal.

---

¡Este protocolo será una herramienta invaluable para nuestro flujo de trabajo!
