import { syncAll } from './core/sync';

/**
 * Punto de entrada principal para el motor de sincronización.
 * Este script invoca la sinfonía de sincronización completa.
 */
const main = () => {
  syncAll();
};

main();
